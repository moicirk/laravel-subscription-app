name: production-actions
run-name: ${{ github.actor }} is learning GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: My first step
        uses: actions/checkout@v5

      - run: echo "My setup is ready!"
